# Guide de Démarrage - Avec Fixes HTTP 500\n\n## ⚡ Démarrage Rapide (3 minutes)\n\n### 1️⃣ Activer l'environnement Python\n```bash\ncd /home/widedwided/moteur_hybride2\nsource .venv/bin/activate\n```\n\n### 2️⃣ Vérifier Ollama (optionnel mais recommandé)\n```bash\n# Diagnostiquer les problèmes potentiels\npython3 monitor_ollama.py\n\n# Pré-charger le modèle\npython3 preload_ollama_model.py\n```\n\n### 3️⃣ Lancer Streamlit\n```bash\nstreamlint run app/streamlit_app2.py\n```\n\nL'app ouvre sur http://localhost:8501\n\n---\n\n## 📋 Checklist de Vérification\n\n### ✅ Avant de lancer\n\n- [ ] Ollama est lancé sur `http://172.31.96.1:11434`\n  ```bash\n  curl http://172.31.96.1:11434/api/tags | head -20\n  ```\n\n- [ ] Le modèle `phi3:mini` est chargé\n  ```bash\n  curl http://172.31.96.1:11434/api/tags | grep phi3\n  ```\n\n- [ ] Assez de mémoire disponible\n  ```bash\n  free -h  # Au moins 1.5 GB libre\n  ```\n\n- [ ] Python 3.10+ activé\n  ```bash\n  python3 --version\n  ```\n\n### ✅ Pendant l'exécution\n\n- [ ] Pas de messages `[E3] HTTP 500` (ou très peu)\n- [ ] Messages `[E3] SLM réponse en X.XXs` apparaissent\n- [ ] L'app continue de fonctionner même avec erreurs SLM\n\n---\n\n## 🚀 Modes de Lancement\n\n### Mode 1: Production (Recommandé)\n```bash\n# Pré-charge le modèle + lance l'app\npython3 preload_ollama_model.py && streamlit run app/streamlit_app2.py\n```\n\n### Mode 2: Avec Monitoring\n```bash\n# Terminal 1: Monitoring\nwatch -n 5 'curl -s http://172.31.96.1:11434/api/tags | jq .'\n\n# Terminal 2: Application\nstreamlint run app/streamlit_app2.py\n```\n\n### Mode 3: Debug\n```bash\n# Logs détaillés\nexport LOG_LEVEL=DEBUG\nstreamlit run app/streamlit_app2.py\n```\n\n---\n\n## 🔧 Configuration Avancée\n\n### Augmenter les Retries (si encore d'erreurs)\n\nModifier `src/e3_slm_fallback.py`:\n```python\n# Ligne ~140\nself.max_retries = 3  # Au lieu de 2\n```\n\n### Augmenter le Timeout\n\nModifier `src/config.py`:\n```python\nOLLAMA_TIMEOUT_SECONDS = int(os.getenv(\"OLLAMA_TIMEOUT_SECONDS\", \"180\"))  # 180s au lieu de 120s\n```\n\nOu en variable d'environnement:\n```bash\nexport OLLAMA_TIMEOUT_SECONDS=180\nstreamlit run app/streamlit_app2.py\n```\n\n### Réduire la Sensibilité du Circuit Breaker\n\nModifier `src/e3_slm_fallback.py`:\n```python\n# Ligne ~50\n_ollama_circuit_breaker = OllamaCircuitBreaker(\n    failure_threshold=3,  # Plus tolérant (3 au lieu de 2)\n    recovery_timeout=30   # Plus de temps pour récupérer\n)\n```\n\n---\n\n## 📊 Monitoring en Temps Réel\n\n### Dashboard simple\n```bash\n# Terminal dédié\nwhile true; do\n  echo \"=== $(date) ===\"\n  curl -s http://172.31.96.1:11434/api/tags | jq '.models[].name' || echo \"Ollama DOWN\"\n  echo \"\"\n  sleep 5\ndone\n```\n\n### Avec Python\n```python\nfrom src.e3_slm_fallback import E3SLMFallback\n\nstats = E3SLMFallback.get_cache_stats()\nprint(f\"Cache hits: {stats['hits']}\")\nprint(f\"Hit rate: {stats['hit_rate_percent']}%\")\n```\n\n---\n\n## ⚠️ Troubleshooting\n\n### Erreur: \"Circuit breaker OUVERT\"\n\n```\n[E3] Circuit breaker OUVERT - SLM désactivé temporairement\n```\n\n**Solution:**\n- Attendre 20-30 secondes (le circuit se ferme automatiquement)\n- Ou relancer l'app avec Ctrl+C puis relancer\n\n### Erreur: \"HTTP 500 (tentative 1/2)\"\n\n```\n[E3] HTTP 500 (tentative 1/2)\n[E3] Attente 2s avant retry...\n```\n\n**Normal!** L'app retry automatiquement. Si ça continue:\n- Vérifier Ollama: `monitor_ollama.py`\n- Redémarrer Ollama\n- Augmenter les ressources\n\n### Erreur: \"Connection error\"\n\n```\n[E3] Connection error: HTTPConnectionPool(...)\n```\n\n**Ollama n'est pas accessible:**\n```bash\n# Vérifier que Ollama est lancé\ncurl http://172.31.96.1:11434/api/version\n\n# Vérifier l'IP (parfois 127.0.0.1 au lieu de 172.31.96.1)\nnetstat -tuln | grep 11434\n```\n\n### Erreur: \"Timeout après 120s\"\n\n```\n[E3] Timeout Ollama (tentative 2/2)\n```\n\n**Modèle trop lent:**\n- Augmenter `OLLAMA_TIMEOUT_SECONDS`\n- Ou réduire le prompt (voir code)\n\n---\n\n## 📈 Amélioration de Performance\n\n### 1. Pré-charger le modèle au démarrage\n```bash\npython3 preload_ollama_model.py\n```\n\n### 2. Réduire le bruit du prompt\nModifier le prompt dans `src/e3_slm_fallback.py` pour être plus court\n\n### 3. Augmenter le cache\n```python\n_slm_cache = SLMCache(max_size=200)  # Au lieu de 50\n```\n\n### 4. Vider le cache si needed\n```python\nE3SLMFallback.clear_cache()  # Réinitialise le cache\n```\n\n---\n\n## 📝 Logs Utiles\n\n### Voir tous les logs E3\n```bash\nstreamlit run app/streamlit_app2.py 2>&1 | grep \"\\[E3\\]\"\n```\n\n### Voir les erreurs détaillées\n```bash\nstreamlit run app/streamlit_app2.py 2>&1 | grep -A 2 \"HTTP 500\"\n```\n\n### Grep par niveau\n```bash\nstreamlit run app/streamlit_app2.py 2>&1 | grep -E \"\\[E3\\].*warning|\\[E3\\].*error\"\n```\n\n---\n\n## 📚 Fichiers de Référence\n\n- **Documentation complète**: [FIX_E3_HTTP500.md](FIX_E3_HTTP500.md)\n- **Diagnostic**: [DIAGNOSTIC_E3.md](DIAGNOSTIC_E3.md)\n- **Code SLM**: [src/e3_slm_fallback.py](src/e3_slm_fallback.py)\n- **Configuration**: [src/config.py](src/config.py)\n\n---\n\n## ✅ Vérification Finale\n\nUne fois lancée, l'app devrait:\n\n1. ✅ Charger les données GeoNames\n2. ✅ Accepter du texte SWIFT\n3. ✅ Parser sans crash même si SLM échoue\n4. ✅ Afficher des logs `[E3]` sans bloque l'affichage\n\nSi tout est OK → **Vous êtes prêt!** 🎉\n\n---\n\n**Version**: 1.0 | **Date**: 2026-04-17 | **Auteur**: Fix E3 HTTP 500\n