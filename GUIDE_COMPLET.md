# 📖 Guide Complet - Erreurs HTTP 500 SLM E3\n\n## 🎯 Résumé Exécutif\n\nVous aviez des erreurs `[E3] HTTP 500` dans votre application Streamlit.\n\n**✅ C'est fixé!** Les fixes incluent:\n- Circuit breaker pour protéger Ollama\n- Backoff exponentiel entre retries\n- Réduction progressive du prompt\n- Logging amélioré\n\n---\n\n## 🚀 Démarrer Immédiatement\n\n### Option 1: Automatique (Recommandé)\n```bash\ncd /home/widedwided/moteur_hybride2\nbash launch_app.sh\n```\nCe script:\n1. Active l'environnement\n2. Diagnostique Ollama\n3. Pré-charge le modèle\n4. Lance les tests\n5. Démarre Streamlit\n\n### Option 2: Manuel\n```bash\ncd /home/widedwided/moteur_hybride2\nsource .venv/bin/activate\nstreamlit run app/streamlit_app2.py\n```\n\n### Option 3: Avec Diagnostic\n```bash\n# Terminal 1: Voir l'état d'Ollama\npython3 monitor_ollama.py\n\n# Terminal 2: Lancer l'app\nstreamlit run app/streamlit_app2.py\n```\n\n---\n\n## 📊 Comprendre ce qui a été changé\n\n### Avant (Problématique)\n```python\ndef _call_slm_optimized(self, party):\n    # Juste 1 retry, pas de gestion d'erreur intelligente\n    for attempt in range(self.max_retries):\n        try:\n            response = requests.post(..., timeout=120)\n            if response.status_code == 200:\n                return self._parse_slm_response(...)  # ✅\n            else:\n                logger.warning(f\"[E3] HTTP {response.status_code}\")  # ❌ Erreur affichée mais pas gérée\n        except:\n            pass\n    return None  # ❌ Failure silencieuse\n```\n\n### Après (Fixé)\n```python\nclass OllamaCircuitBreaker:  # ✅ Nouvelle classe\n    def can_attempt(self) -> bool:\n        # Vérifie si on peut faire une tentative\n        if not self.is_open:\n            return True\n        if (time.time() - self.last_failure_time) > self.recovery_timeout:\n            self.is_open = False  # ✅ Ferme le circuit\n        return not self.is_open\n\ndef _call_slm_optimized(self, party):\n    # ✅ Vérification du circuit breaker\n    if not _ollama_circuit_breaker.can_attempt():\n        logger.warning(\"[E3] Circuit breaker OUVERT\")\n        return None\n    \n    for attempt in range(self.max_retries):\n        try:\n            response = requests.post(...)\n            if response.status_code == 200:\n                _ollama_circuit_breaker.record_success()  # ✅ Réinitialiser\n                return self._parse_slm_response(...)\n            else:\n                _ollama_circuit_breaker.record_failure()  # ✅ Compter les erreurs\n                # ✅ Réduire le prompt et attendre\n                payload[\"options\"][\"num_predict\"] -= 30\n                time.sleep(min(5, (attempt + 1) * 2))\n        except Exception as e:\n            _ollama_circuit_breaker.record_failure()  # ✅ Compter les erreurs\n            logger.error(f\"[E3] {e}\")\n    \n    return None  # Échec après retries\n```\n\n---\n\n## 🔍 Fichiers Modifiés\n\n### Principal\n- **[src/e3_slm_fallback.py](src/e3_slm_fallback.py)**\n  - L. 20-60: Classe `OllamaCircuitBreaker`\n  - L. 61: Instance globale du circuit breaker\n  - L. 235-305: Méthode `_call_slm_optimized()` améliorée\n\n### Utilitaires Créés\n- **[monitor_ollama.py](monitor_ollama.py)** - Diagnostic complet d'Ollama\n- **[preload_ollama_model.py](preload_ollama_model.py)** - Pré-charge le modèle\n- **[test_e3_quick.py](test_e3_quick.py)** - Tests des fixes\n- **[launch_app.sh](launch_app.sh)** - Lancement automatique\n\n### Documentation\n- **[FIX_E3_HTTP500.md](FIX_E3_HTTP500.md)** - Détails complets des fixes\n- **[QUICKSTART.md](QUICKSTART.md)** - Guide de démarrage\n- **[RESUME_FIXES.md](RESUME_FIXES.md)** - Résumé\n- **[DIAGNOSTIC_E3.md](DIAGNOSTIC_E3.md)** - Diagnostic détaillé\n\n---\n\n## 🧪 Tester les Fixes\n\n### Test 1: Diagnostic Ollama\n```bash\npython3 monitor_ollama.py\n```\n\nDoit afficher:\n```\n✅ Ollama accessible\n✅ Modèles disponibles\n✅ Test /api/generate: 3/3 réussis\n✅ Ollama fonctionne normalement\n```\n\n### Test 2: Fixes E3\n```bash\npython3 test_e3_quick.py\n```\n\nDoit afficher:\n```\n✅ SLM fallback détecté\n✅ Appel en 47.0s\n✅ Circuit breaker fonctionne\n✅ Tests terminés\n```\n\n### Test 3: Application Complète\n```bash\nstreamlit run app/streamlit_app2.py\n```\n\nDoit afficher (pas d'erreurs critiques):\n```\n✅ GeoNames disponible pour validation\n[E3] Appel SLM en cours\n[E3] SLM réponse en 2.5s\n[E3] SLM terminé\n```\n\n---\n\n## ⚠️ Comportements Attendus\n\n### Normal: Aucune Erreur\n```\n[E3] Appel SLM en cours\n[E3] SLM réponse en 3.2s\n[E3] SLM terminé\n```\n\n### Normal: Erreur Isolée\n```\n[E3] HTTP 500 (tentative 1/2)\n[E3] Attente 2s avant retry...\n[E3] SLM réponse en 1.8s\n[E3] SLM terminé\n```\n→ Le système se rétablit automatiquement\n\n### Normal: Circuit Breaker Activé\n```\n[E3] HTTP 500 (tentative 1/2)\n[E3] HTTP 500 (tentative 2/2)\n[E3] Circuit breaker OUVERT après 2 erreurs\n[E3] SLM a échoué, conservation du résultat original\n```\n→ L'app continue de fonctionner sans SLM pendant 20 secondes\n\n### Récupération\n```\n(après 20 secondes)\n[E3] Circuit breaker tentative de FERMETURE\n[E3] Appel SLM en cours\n[E3] SLM réponse en 2.1s\n```\n→ Retour à la normale\n\n---\n\n## 🔧 Configuration Avancée\n\n### Augmenter la Tolérance (moinscircuit breaks)\n\nModifier [src/e3_slm_fallback.py](src/e3_slm_fallback.py#L61):\n```python\n_ollama_circuit_breaker = OllamaCircuitBreaker(\n    failure_threshold=3,  # 3 erreurs au lieu de 2\n    recovery_timeout=30   # 30s de pause au lieu de 20s\n)\n```\n\n### Augmenter le Timeout\n\n```bash\nexport OLLAMA_TIMEOUT_SECONDS=180  # 180s au lieu de 120s\nstreamlit run app/streamlit_app2.py\n```\n\nOu modifier [src/config.py](src/config.py):\n```python\nOLLAMA_TIMEOUT_SECONDS = 180\n```\n\n### Augmenter les Retries\n\nModifier [src/e3_slm_fallback.py](src/e3_slm_fallback.py#L140):\n```python\nself.max_retries = 3  # 3 retries au lieu de 2\n```\n\n### Désactiver le Circuit Breaker\n\n```python\n_ollama_circuit_breaker = OllamaCircuitBreaker(\n    failure_threshold=999,  # Pratiquement jamais activé\n    recovery_timeout=1\n)\n```\n\n---\n\n## 🚨 Troubleshooting\n\n### Erreur: \"Circuit breaker OUVERT\"\n\n**Symptôme**:\n```\n[E3] Circuit breaker OUVERT - SLM désactivé temporairement\n```\n\n**Cause**: Trop d'erreurs Ollama\n\n**Solution**:\n1. Attendre 20-30 secondes (récupération auto)\n2. Ou relancer l'app: `Ctrl+C` + relancer\n3. Ou redémarrer Ollama\n\n### Erreur: \"Connection error\"\n\n**Symptôme**:\n```\n[E3] Connection error: HTTPConnectionPool(...)\n```\n\n**Cause**: Ollama n'est pas accessible\n\n**Solution**:\n```bash\n# Vérifier Ollama\ncurl http://172.31.96.1:11434/api/version\n\n# Ou tester\npython3 monitor_ollama.py\n```\n\n### Erreur: \"Timeout after 120s\"\n\n**Symptôme**:\n```\n[E3] Timeout Ollama (tentative 2/2, timeout=120s)\n```\n\n**Cause**: Modèle trop lent\n\n**Solution**:\n- Augmenter le timeout: `export OLLAMA_TIMEOUT_SECONDS=180`\n- Ou réduire la taille du prompt\n\n### Performance Lente\n\n**Symptôme**:\n- SLM prend 30+ secondes\n- Ou erreurs sporadiques\n\n**Cause**: Ressources système limitées\n\n**Solution**:\n```bash\n# Vérifier les ressources\nfree -h\ndf -h /\nps aux | grep ollama\n\n# Ou\npython3 monitor_ollama.py\n```\n\n---\n\n## 📈 Performance Attendue\n\n| Métrique | Valeur | Notes |\n|----------|--------|-------|\n| Temps réponse SLM | 1-5s | Normal |\n| Temps réponse (timeout) | 2-10s | Peut être plus long |\n| Taux succès | >90% | Après fix |\n| Temps circuit breaker | 20s | Avant récupération |\n| Retry total | 2 tentatives | Max 5s d'attente |\n\n---\n\n## 📚 Lectures Recommandées\n\n1. **[FIX_E3_HTTP500.md](FIX_E3_HTTP500.md)** - Pour comprendre les détails techniques\n2. **[QUICKSTART.md](QUICKSTART.md)** - Pour apprendre à démarrer\n3. **[src/e3_slm_fallback.py](src/e3_slm_fallback.py)** - Pour voir le code\n\n---\n\n## ✅ Checklist Final\n\nAvant de déployer:\n\n- [ ] Ollama est lancé et accessible\n- [ ] `python3 monitor_ollama.py` retourne OK\n- [ ] `python3 test_e3_quick.py` passe tous les tests\n- [ ] `streamlit run app/streamlit_app2.py` démarre sans erreurs\n- [ ] Les logs E3 affichent les retries et fallbacks\n- [ ] L'app continue même si SLM échoue\n\n---\n\n## 🎉 Vous Êtes Prêt!\n\nLancer l'application:\n\n```bash\nbash launch_app.sh\n```\n\nOu:\n\n```bash\nstreamlit run app/streamlit_app2.py\n```\n\n**Bon développement!** 🚀\n\n---\n\n**Dernière mise à jour**: 2026-04-17  \n**Version**: 1.0  \n**Statut**: ✅ Production Ready\n